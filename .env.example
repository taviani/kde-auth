# Server (local dev)
PORT=3001
ISSUER=http://localhost:3001

# Database (local dev — matches docker-compose.yml)
DATABASE_URL=postgres://kde_auth:kde_auth@localhost:5433/kde_auth?sslmode=disable

# Registration
REGISTRATION_OPEN=true
